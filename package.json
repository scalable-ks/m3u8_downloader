{
  "name": "rn-android-hls",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-strip-types",
    "test:watch": "node --test --watch --experimental-strip-types"
  }
}
